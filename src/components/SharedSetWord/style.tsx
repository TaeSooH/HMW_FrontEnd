import styled from "styled-components";
import * as S from "../Word/style";

export const WordContainer = styled(S.WordContainer)``;
export const Times = styled(S.Times)``;
export const WordBox = styled(S.WordBox)``;
export const Word = styled(S.Word)``;
export const RightSide = styled(S.RightSide)``;
