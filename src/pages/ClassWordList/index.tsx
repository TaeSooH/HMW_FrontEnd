import React from "react";
import Header from "../../components/Header";
import SharedSetWord from "../SharedSetWord";
import * as S from "./style";

const index = ({ name }) => {
  return <SharedSetWord name={name} />;
};

export default index;
